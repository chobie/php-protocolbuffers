package Tutorial;

//import "php_options.proto";
//option (php).generate_getter = true;
//option (php).generate_setter = true;

message User {
    required int32 id = 1;
    optional float offense = 2;
    optional string name = 3;
}

message AddressBook {
    repeated User user = 1;
}
